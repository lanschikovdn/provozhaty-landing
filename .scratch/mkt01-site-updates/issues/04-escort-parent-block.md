# 04 — Блок для родителей сопровождающего в карточке «Стать провожатым»

**Blocked by:** 03-cta-by-house-mechanic
**Status:** ready-for-agent

## Что сделать

Файл: `C:\Users\Pro X\Провожатый\сайт\landing\index.html`, секция `#roles`, карточка `.role-card.role-card-accent`.

Сейчас:
```html
<div class="role-card role-card-accent reveal">
  <svg class="role-icon" ...>...</svg>
  <h3>Хочу сопровождать</h3>
  <p>Я старшеклассник, студент или родитель — готов провожать детей своей школы за плату.</p>
  <a href="#cta" class="btn btn-outline btn-sm">Стать провожатым</a>
</div>
```

По отчёту МКТ-01 (`.scratch/mkt01-site-updates/spec.md`, правка №4): сопровождающий часто несовершеннолетний, его родитель должен дать согласие (ст. 26 ГК) — и именно он будет гуглить, куда нанимается ребёнок. Нужен короткий адресный блок именно для этого родителя, не как канал набора, а как снятие сомнений.

Добавить внутрь карточки, после `<p>` и перед кнопкой, короткий список из 4 пунктов (используй `<ul>` с простыми `<li>`, без новых CSS-классов — использовать существующие текстовые стили карточки, добавить минимальный CSS только если список выглядит некрасиво без отступов/маркеров):

```html
<ul class="escort-parent-notes">
  <li>Официальный договор и налог самозанятого (НПД)</li>
  <li>Страхование на каждом маршруте</li>
  <li>Обязательное обучение до допуска к маршрутам</li>
  <li>Чётко: сопровождающий не остаётся с ребёнком дома и не забирает его без согласованного списка</li>
</ul>
```

Добавь короткий вводный текст перед списком, адресующий именно родителя подростка, например: `<p class="escort-parent-lead">Родителям несовершеннолетнего сопровождающего — коротко о том, на что вы соглашаетесь:</p>` (текст можно скорректировать по тону).

В `landing/css/style.css` добавить минимальные стили для `.escort-parent-lead` и `.escort-parent-notes` (например: `font-size:13px`, отступы, маркер-точка в цвет `var(--mint)` на тёмном фоне карточки — карточка `.role-card-accent` тёмная, текст `var(--paper-muted)`) — не менять существующие правила `.role-card`/`.role-card-accent`, только добавить новые точечные селекторы.

## Проверка

Открыть лендинг локально, убедиться что карточка «Хочу сопровождать» не переполнена/не съехала на мобильной ширине (375px) и на десктопе, текст читаем на тёмном фоне карточки. Проверить все три темы (А/Б/В) — новый список не должен визуально ломаться ни в одной. Commit на русском, добавить `landing/index.html` и `landing/css/style.css`. `Status: done` в этом файле тикета.
