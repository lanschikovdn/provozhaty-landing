# 05 — QA всех правок + обновить документы

**Blocked by:** 04-escort-parent-block
**Status:** done

## Что сделать

1. Прочитать целиком `landing/index.html` и `landing/css/style.css` — убедиться, что все 4 правки (тикеты 01-04) на месте и согласованы друг с другом (например, что новый CTA-текст «по домам» не противоречит формулировкам в hero/футере).
2. Прогнать чек-лист приёмки из `.scratch/mkt01-site-updates/spec.md`, раздел «Приёмка» — каждый пункт.
3. Поднять локальный сервер (`python -m http.server 8792 --directory "C:\Users\Pro X\Провожатый\сайт\landing"` либо использовать существующий launch.json конфиг `provozhaty-landing`) и проверить все три темы (А/Б/В), десктоп и мобильную ширину (375px) — особенно карточку «Стать провожатым» (новый список) и блок безопасности (новый пункт про 60 минут).
4. Если найдёшь мелкие несоответствия — почини точечно (не архитектурно).
5. Обновить `C:\Users\Pro X\Провожатый\сайт\PROGRESS.md` — добавить новую секцию/таблицу для фичи `mkt01-site-updates` с 5 тикетами и статусом `done` у всех.
6. В `C:\Users\Pro X\Провожатый\сайт\CONTEXT.md` добавить короткую запись о том, что лендинг обновлён по отчёту МКТ-01 (дата, 4 правки одной строкой), не переписывая существующие разделы «Фазы».
7. Commit на русском для всех правок этого тикета (документы + если были мелкие фиксы кода).
8. `Status: done` в этом файле тикета.

## Проверка

Все пункты приёмки из spec.md — зелёные. Локальный сервер запущен и отвечает (не останавливать его после проверки — он понадобится пользователю для просмотра).
