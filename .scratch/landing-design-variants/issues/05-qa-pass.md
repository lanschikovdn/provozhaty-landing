# 05 — QA всех трёх тем + обновить документы

**Blocked by:** 04-animation-presets
**Status:** done

## Что сделать

1. Открыть `landing/index.html` через `provozhaty-landing` (launch.json, порт 8792) или `python -m http.server` из `landing/`. Прогнать все критерии приёмки из `.scratch/landing-design-variants/spec.md`, раздел «Приёмка»:
   - Дефолт `warm` идентичен Фазе 1.
   - Клик Б/В — мгновенная перекраска, скролл не сбрасывается.
   - Reload после выбора темы — тема сохраняется.
   - Hero-видео и скраббинг работают одинаково во всех темах.
   - Reveal-анимации визуально отличаются по стилю между `soft` и `bold`.
   - Мобильная ширина (375px) не сломана ни в одной теме, переключатель не перекрывает форму/CTA.
   - Форма и чекбокс 152-ФЗ работают во всех темах.
2. Если что-то из списка не проходит — исправить (небольшие точечные правки CSS/JS, не переписывать архитектуру).
3. Дополнить `C:\Users\Pro X\Провожатый\сайт\references\design-system.md` новым разделом «Фаза 2 — варианты тем» с кратким описанием трёх тем (`warm`/`soft`/`bold`) и таблицей токенов (можно сослаться на `.scratch/landing-design-variants/spec.md` вместо дублирования таблиц целиком, если это уместнее).
4. В `C:\Users\Pro X\Провожатый\сайт\CONTEXT.md` отметить пункт «2. 🔄 Несколько принципиально разных визуальных вариантов» как `✅` вместо `🔄`.

## Проверка

Все пункты чек-листа приёмки из спеки — зелёные. `design-system.md` и `CONTEXT.md` обновлены и не содержат противоречий с фактическим состоянием кода.
